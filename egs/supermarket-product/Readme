using run.sh to train the system, decode.sh can decode the test audio by all the algorithm we have trained the system
but decode_test.sh can only decode by SGMM and fMLLR algorithm
path_decode.sh and decode_test.sh is used for interface call
